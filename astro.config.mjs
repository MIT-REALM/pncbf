import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    // site: "https://mit-realm.github.io",
    site: "https://oswinso.github.io",
    base: "/pncbf"
});
